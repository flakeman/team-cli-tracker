# SDD-05 Plan

## Phase 1 - Board Table UX
- Define table layout contract (headers, widths, counts, truncation).
- Implement deterministic renderer for plain format.
- Add regression tests for representative board snapshots.

## Phase 2 - 3-VPS Smoke Validation
- Produce executable smoke checklist for `main`.
- Run auth/key/discovery/churn smoke on 3 VPS.
- Capture outputs and pass/fail evidence.

## Phase 3 - Release Baseline
- Prepare `CHANGELOG.md` sections for current milestone.
- Define `v0.x` tagging convention and release notes template.
- Create release checklist with required pre-tag verifications.

## Phase 4 - Sign-Off
- Verify docs link consistency (`README`, `ROADMAP`, SDD index references).
- Confirm all SDD-05 acceptance criteria with dated evidence.
